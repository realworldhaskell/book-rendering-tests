The `main` function in the `Main` module is the place every Haskell program begins its execution.

> module Main where
>
> import Lib

This is an import statement


> main :: IO ()
> main = someFunc

This is a test
